<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    >

    <Space
        android:id="@+id/fragment_hub_space"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="#00000000"
        android:layout_alignParentBottom="true"
        />

    <LinearLayout
        android:id="@+id/fragment_hub_chat_main_type_layout"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_above="@id/fragment_hub_space"
        android:orientation="horizontal"
        >

        <EditText
            android:id="@+id/messageEditText"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical"
            android:background="#00000000"
            android:layout_weight="1"
            android:textSize="14sp"
            android:textColor="@color/textMain"
            android:inputType="textCapSentences"
            />

        <Button
            android:id="@+id/sendButton"
            android:layout_width="72dp"
            android:layout_height="match_parent"
            android:layout_gravity="bottom"
            android:enabled="false"
            android:textColor="@color/textMain"
            android:background="#40000000"
            android:typeface="sans"
            android:textSize="12sp"
            android:text="send"/>

    </LinearLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_above="@id/fragment_hub_chat_main_type_layout"
        >

        <ListView
            android:id="@+id/fragment_hub_chat_roomlistview"
            android:layout_width="48dp"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:divider="@android:color/transparent"
            tools:listitem="@layout/chat_rooms"
            >

        </ListView>

        <ListView
            android:id="@+id/fragment_hub_chat_main_messageListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:stackFromBottom="true"
            android:divider="@android:color/transparent"
            android:transcriptMode="alwaysScroll"
            tools:listitem="@layout/chat_message"
            android:layout_toLeftOf="@id/fragment_hub_chat_roomlistview"
            />

    </RelativeLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"/>

</RelativeLayout>